


typedef struct Trip{
  int trip_id;
  double emission_amount;
  double emission_score;
  double prise_amount;
  double price_score;
  double time_amount;
  double time_score;
  double overall_score;
} Trip;

int Large(Trip* arr, size_t arr_size){
  return 0;
}

int Small(Trip* arr, size_t arr_size){
  return 0;
}

void Evaluate(){
  Trip* trip_arr = NULL;

  trip_arr[0].trip_id = 23
  size_t trip_arr_size_elements = sizeof(trip_arr) / sizeof(Trip);

  Large(trip_arr, trip_arr_size_elements);
  Small(trip_arr, trip_arr_size_elements);

}




